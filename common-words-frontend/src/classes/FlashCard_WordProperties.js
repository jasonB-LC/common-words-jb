export default class FlashCard_WordProperties {
	constructor(id, flashCardId, wordPropertyId, isFront) {
        this.id = id;
        this.flashCardId = flashCardId;
        this.wordPropertyId = wordPropertyId;
        this.isFront = isFront;
    }
}
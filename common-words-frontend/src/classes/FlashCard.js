export default class FlashCard {
	constructor(id, daysUntilDue, dateOfLastReview) {
        this.id = id;
        this.daysUntilDue = daysUntilDue;
        this.dateOfLastReview = dateOfLastReview;
    }
}